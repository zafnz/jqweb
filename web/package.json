{
  "name": "jqweb-web",
  "private": true,
  "engines": {
    "node": ">=24.12.0"
  },
  "scripts": {
    "build": "node build.mjs",
    "check": "tsc --noEmit",
    "test": "node --test"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "typescript": "7.0.2"
  }
}
