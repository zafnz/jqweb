{
  "name": "jqweb-web",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.12.0"
  },
  "scripts": {
    "build": "node build.ts",
    "check": "tsc --noEmit",
    "test": "node --test test/unit/*.test.ts",
    "test:browser": "playwright test --config=test/browser/playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@types/node": "24.13.4",
    "esbuild": "0.28.2",
    "typescript": "7.0.2"
  }
}
