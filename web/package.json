{
  "name": "jqweb-web",
  "private": true,
  "engines": {
    "node": ">=24.12.0"
  },
  "scripts": {
    "build": "node build.mjs",
    "check": "tsc --noEmit",
    "test": "node --test",
    "test:browser": "playwright test --config=browser-test/playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "esbuild": "0.28.2",
    "typescript": "7.0.2"
  }
}
