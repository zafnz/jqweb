/* Only the browser tests see these injected helpers. Their type is inferred
   from the checked JavaScript that Playwright sends to the page unchanged. */
declare var __t: ReturnType<typeof browserHelpers>;
declare var __sources: string[];
